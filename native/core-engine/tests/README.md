# Core Engine Tests

## `engine_test.cpp`

Small smoke test for the portable C++ layer.

It calls `generic_engine::runEngine`, validates the result with assertions, and prints the JSON output.

Run from the project root:

```bash
npm run test:engine
```
