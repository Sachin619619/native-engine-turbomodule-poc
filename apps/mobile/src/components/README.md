# Components

## `ResultCard.tsx`

Small React Native display component used by `App.tsx`.

It only renders a label and value. It does not know about native modules, Android, JNI, or C++.

Keeping it simple makes the UI layer separate from the native engine flow.
